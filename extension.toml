id = "token-counter"
name = "Token Counter"
version = "0.0.1"
schema_version = 1
authors = ["dev"]
description = "Displays LLM token counts (GPT-4, GPT-4o, Claude estimate) as a CodeLens at the top of your files"

[language_servers.token-counter]
name = "Token Counter"
languages = ["Markdown", "Plain Text", "TOML", "YAML", "JSON"]
